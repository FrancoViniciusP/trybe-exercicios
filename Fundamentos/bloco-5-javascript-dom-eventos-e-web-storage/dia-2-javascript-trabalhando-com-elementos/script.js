
      let numArray = ['um', 'dois', 'três', 'quatro', 'cinco', 'seis', 'sete', 'oito', 'nove', 'dez'];
      for (let i = 0; i < numArray; i += 1) {
        let temp = numArray[i]
        
      }
// lista.appendChild(addList(temp));